[project]
name = "openatc-protocol"
version = "0.1.0"
description = "WebSocket/RPC schemas and protocol definitions"
requires-python = ">=3.12"
dependencies = [
    "openatc-common",
    "pydantic>=2.7",
]

[project.optional-dependencies]
grpc = [
    "grpcio>=1.64",
    "grpcio-tools>=1.64",
]
